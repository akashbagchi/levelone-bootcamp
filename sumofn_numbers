#include<stdio.h>

int main()
{
	int n;
float sum=0, a;
	printf("\nHow many numbers do you want to add? :");
	scanf("%d", &n);
	
	for(int i=0; i<n; i++)
	{
		printf("\nEnter a number :");
		scanf("%f", a);
		sum+=a;
	}

	printf("\nThe sum of the numbers you entered is %f", sum);
	return 0;
}
